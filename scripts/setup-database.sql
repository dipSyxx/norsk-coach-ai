-- DEPRECATED: schema bootstrap is now managed by Prisma migrations.
--
-- Use:
--   pnpm prisma migrate deploy
--   pnpm prisma generate
--
-- This file is intentionally a no-op placeholder to avoid accidentally
-- maintaining parallel SQL schema sources.
SELECT 'Use Prisma migrations (prisma/migrations) as the single source of truth.' AS note;
